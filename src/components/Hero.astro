---
import AtencionInmediataCard from "./AtencionInmediataCard.jsx";

const telefonoPrincipal = "15-6218-1863";
const whatsappNumber =
  import.meta.env.PUBLIC_WHATSAPP_NUMBER ?? "5491162181863";
---

<section
  class="mb-10 grid gap-8 rounded-3xl bg-[radial-gradient(circle_at_top,_#7c2d12_0,_#020617_55%,_#020617_100%)] p-6 shadow-2xl ring-1 ring-red-900/40 md:grid-cols-[minmax(0,1.4fr)_minmax(0,1fr)] md:p-10"
>
  <!-- Columna izquierda: mensaje principal -->
  <div class="flex flex-col justify-center gap-5">
    <div class="flex flex-wrap gap-3 text-xs font-semibold">
      <span
        class="inline-flex items-center rounded-full bg-red-700 px-3 py-1 text-amber-100"
      >
        Cerrajería Maresca
      </span>
      <span
        class="inline-flex items-center rounded-full bg-slate-900/70 px-3 py-1 text-slate-100"
      >
        Urgencias · La Boca y alrededores
      </span>
    </div>

    <h1
      class="text-3xl font-semibold tracking-tight text-slate-50 md:text-4xl lg:text-5xl"
    >
      Cerrajería en La Boca,
      <span class="block">para viviendas y comercios.</span>
    </h1>

    <p
      class="max-w-xl text-sm leading-relaxed text-slate-100/90 md:text-base"
    >
      Abrimos puertas, reparamos cerraduras y trabajamos con puertas de
      cristal, frentes vidriados, herrajes especiales y apertura de vehículos.
      Atendemos desde nuestro local en Palos 721, Local 7.
    </p>

    <ul class="space-y-1 text-sm text-slate-100/90">
      <li>• Emergencias sin turno, todos los días.</li>
      <li>• Apertura de puertas sin rotura cuando es posible.</li>
      <li>• Apertura de vehículos y cierra puertas de piso.</li>
    </ul>

    <div class="mt-3 flex flex-col gap-3 sm:flex-row">
      <a
        href={`tel:${telefonoPrincipal.replace(/[^0-9]/g, "")}`}
        class="inline-flex items-center justify-center rounded-full bg-amber-400 px-5 py-2.5 text-sm font-semibold text-slate-950 shadow-lg transition hover:bg-amber-300 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-400/90 focus-visible:ring-offset-2 focus-visible:ring-offset-slate-950"
      >
        Llamar ahora · {telefonoPrincipal}
      </a>
      <a
        href="#contacto"
        class="inline-flex items-center justify-center rounded-full border border-emerald-500/70 px-5 py-2.5 text-sm font-semibold text-emerald-100 shadow-sm transition hover:bg-emerald-500/10 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-400/90 focus-visible:ring-offset-2 focus-visible:ring-offset-slate-950"
      >
Contactar ahora      </a>
    </div>

    <div
      class="mt-3 flex flex-col gap-1 text-[0.75rem] text-slate-200 sm:flex-row sm:gap-6"
    >
      <p>
        <span class="font-semibold">Guardia de cerrajería.</span> Priorizamos
        urgencias en La Boca y zonas cercanas.
      </p>
      <p>
        <span class="font-semibold">Local a la calle.</span> Palos 721 · Local 7
        · Referencia: zona La Boca.
      </p>
    </div>
  </div>

  <!-- Columna derecha: panel de atención inmediata (React island) -->
  <div class="flex items-center justify-center">
    <AtencionInmediataCard client:visible whatsappNumber={whatsappNumber} />
  </div>
</section>
