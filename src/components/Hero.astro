---
const telefonoPrincipal = "15-6218-1863";
const telefonoSanitized = telefonoPrincipal.replace(/[^0-9]/g, "");

// Usa el mismo número que el botón flotante
const whatsappNumber = import.meta.env.PUBLIC_WHATSAPP_NUMBER ?? "";
const whatsappUrl = whatsappNumber ? `https://wa.me/${whatsappNumber}` : null;
---

<section
  aria-labelledby="hero-title"
  class="relative mb-10 overflow-hidden rounded-3xl border border-red-900/50 bg-slate-950/95 shadow-2xl"
>
  {/* Glow / decorativo de fondo */}
  <div
    class="pointer-events-none absolute -left-40 top-[-6rem] h-80 w-80 rounded-full bg-red-600/25 blur-3xl md:-left-10"
    aria-hidden="true"
  ></div>
  <div
    class="pointer-events-none absolute -right-32 bottom-[-6rem] h-72 w-72 rounded-full bg-amber-400/10 blur-3xl md:-right-10"
    aria-hidden="true"
  ></div>

  <div
    class="relative grid gap-8 p-6 bg-linear-to-br from-slate-950 via-red-950/40 to-slate-950 md:grid-cols-[minmax(0,1.5fr)_minmax(0,1fr)] md:p-10"
  >
    {/* Columna izquierda: texto principal */}
    <div class="flex flex-col justify-center gap-5">
      {/* Eyebrow + badges */}
      <div class="flex flex-wrap items-center gap-2 text-[0.7rem] uppercase tracking-wide">
        <span class="rounded-full bg-red-700/80 px-3 py-1 text-[0.65rem] font-semibold text-slate-50">
          Cerrajería Maresca
        </span>
        <span class="rounded-full border border-red-500/50 bg-slate-950/70 px-3 py-1 text-[0.65rem] text-red-100">
          Urgencias 24 horas · La Boca
        </span>
      </div>

      <div>
        <h1
          id="hero-title"
          class="text-balance text-3xl font-semibold tracking-tight text-slate-50 md:text-4xl"
        >
          Cerrajería 24&nbsp;horas en La Boca, para viviendas y comercios.
        </h1>
        <p class="mt-3 text-sm leading-relaxed text-slate-100/90 md:text-base">
          Abrimos puertas, reparamos cerraduras y trabajamos con puertas de
          cristal, frentes vidriados, herrajes especiales y apertura de
          vehículos. Atendemos desde nuestro local en Palos 721, Local 7.
        </p>
      </div>

      <ul class="space-y-1 text-sm text-slate-100/90">
        <li>• Emergencias sin turno, todos los días.</li>
        <li>• Apertura de puertas sin rotura cuando es posible.</li>
        <li>• Apertura de vehículos y cierra puertas de piso.</li>
      </ul>

      {/* CTAs */}
      <div class="mt-3 flex flex-col gap-3 sm:flex-row sm:items-center">
        <a
          href={`tel:${telefonoSanitized}`}
          class="inline-flex items-center justify-center rounded-full bg-amber-400 px-5 py-2.5 text-sm font-semibold text-slate-950 shadow-lg transition hover:bg-amber-300 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-400/80 focus-visible:ring-offset-2 focus-visible:ring-offset-slate-950"
        >
          Llamar ahora · {telefonoPrincipal}
        </a>

        {whatsappUrl ? (
          <a
            href={whatsappUrl}
            target="_blank"
            rel="noreferrer"
            class="inline-flex items-center justify-center rounded-full border border-emerald-400/70 bg-slate-950/60 px-4 py-2 text-xs font-medium text-emerald-200 shadow-sm transition hover:bg-slate-900/80 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-400/80 focus-visible:ring-offset-2 focus-visible:ring-offset-slate-950"
          >
            Chatear por WhatsApp
          </a>
        ) : (
          <p class="text-xs text-slate-300">
            También podés escribirnos por WhatsApp desde el botón flotante.
          </p>
        )}
      </div>

      <div class="mt-1 flex flex-wrap gap-4 text-[0.7rem] text-slate-300">
        <div>
          <span class="block text-slate-100">Atención inmediata</span>
          <span>Prioridad para urgencias en La Boca y zonas cercanas.</span>
        </div>
        <div>
          <span class="block text-slate-100">Local a la calle</span>
          <span>Palos 721 · Local 7 · Referencia: zona La Boca.</span>
        </div>
      </div>
    </div>

    {/* Columna derecha: card visual / status */}
    <div class="flex items-center justify-center">
      <div
        class="relative flex h-full w-full max-w-xs flex-col justify-between rounded-2xl border border-red-500/40 bg-linear-to-br from-red-600/30 via-slate-950 to-slate-900/95 p-4 text-xs text-slate-50 shadow-xl sm:max-w-sm"
        aria-hidden="true"
      >
        {/* Header mini-panel */}
        <div class="flex items-center justify-between gap-2">
          <div>
            <p class="text-[0.7rem] uppercase tracking-wide text-red-100/90">
              Guardia de cerrajería
            </p>
            <p class="text-sm font-semibold">Operativa 24 hs</p>
          </div>
          <div class="flex flex-col items-end text-[0.65rem] text-amber-100">
            <span class="rounded-full bg-amber-400/10 px-2 py-0.5">
              Urgencias activas
            </span>
            <span class="mt-1 text-[0.6rem] text-amber-200/80">
              Atención inmediata
            </span>
          </div>
        </div>

        {/* Lista de "casos" como si fuera panel interno */}
        <div class="mt-4 space-y-2 rounded-xl bg-slate-950/70 p-3">
          <div class="flex items-center justify-between gap-2">
            <div>
              <p class="text-[0.7rem] text-slate-300">
                Vivienda · Puerta principal
              </p>
              <p class="text-[0.7rem] text-slate-100">
                Llave puesta del lado de adentro
              </p>
            </div>
            <span class="rounded-full bg-red-500/20 px-2 py-0.5 text-[0.6rem] text-red-100">
              En curso
            </span>
          </div>
          <div class="flex items-center justify-between gap-2 border-t border-slate-800 pt-2">
            <div>
              <p class="text-[0.7rem] text-slate-300">
                Comercio · Puerta de cristal
              </p>
              <p class="text-[0.7rem] text-slate-100">
                Cerradura trabada al cerrar
              </p>
            </div>
            <span class="rounded-full bg-emerald-500/15 px-2 py-0.5 text-[0.6rem] text-emerald-100">
              Asignado
            </span>
          </div>
        </div>

        {/* Footer panel */}
        <div class="mt-4 flex items-center justify-between gap-2 text-[0.65rem] text-slate-200">
          <div>
            <p class="font-semibold text-amber-200">
              Llamá ahora si te quedaste afuera.
            </p>
            <p class="text-slate-300/90">
              Contanos si es vivienda, comercio o vehículo y la zona aproximada.
            </p>
          </div>
          <div class="flex h-10 w-10 items-center justify-center rounded-full bg-amber-400/90 text-slate-950">
            24h
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
